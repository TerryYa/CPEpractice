#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <cmath>

using namespace std;

int main(){
	int s,d;
	while(scanf("%d%d",&s, &d) != EOF){
		int day=0;
		while(day<d){
			day += s++;
		}
		cout << s-1 << endl;
	}
	return 0;
}
