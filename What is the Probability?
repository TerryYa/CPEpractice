#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <cmath>

using namespace std;

int main(){
	int n;
	while(scanf("%d",&n) != EOF){
		int num,no;
		double p;
		cin >> num >> p >> no;
		printf("%.4f\n",p==0?p:p*pow(1-p,no-1)/(1-pow(1-p,num)));
	}
	return 0;
}
