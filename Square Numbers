#include <iostream>
#include <cstdlib>
#include <cstdio>

using namespace std;

int main(){			
	int a,b;
	while((scanf("%d%d", &a,&b) != EOF ) && (a || b)){
		int c1=0,c2=0;
		for (long int i=1;i*i<a;i++) c1++;
		for (long int i=1;i*i<=b;i++) c2++;
		cout << c2-c1 << endl;
	}
	return 0;
}
